# ✅ СИСТЕМА ЗАПИСУ НА МАНІКЮР ГОТОВА!

## 🎉 Що створено:

### 1. **Telegram бот** ✅
- Запущено на твоєму комп'ютері
- Токен: `8580673971:AAEkJYiGUf5lCniw6bUoN5tEro2x8MxU5E8`
- Логування всіх дій
- Команди: /start, /help, /mybookings

### 2. **База даних SQLite** ✅
- Файл: `C:\Users\kazmi\manicure-booking\data\bookings.db`
- 3 майстри з рейтингами
- 8 послуг з цінами
- Таблиці для записів та користувачів

### 3. **Mini App** ✅
- URL: **https://webapp-xi-smoky.vercel.app**
- Інтерактивний календар
- Вибір майстра та послуги
- Вибір вільного часу
- Підтвердження запису

---

## 🚀 ОСТАННІЙ КРОК: Налаштуй Menu Button

### Відкрий Telegram → @BotFather:

```
/mybots
```

→ Вибери свого бота  
→ **Bot Settings**  
→ **Menu Button**  
→ **Edit Menu Button URL**  
→ Введи:

```
https://webapp-xi-smoky.vercel.app
```

→ Введи текст кнопки:

```
📅 Записатися
```

---

## ✅ Після налаштування:

1. Відкрий чат зі своїм ботом в Telegram
2. Побачиш кнопку **☰** (меню) знизу біля поля вводу
3. Натисни на неї
4. Вибери "📅 Записатися"
5. **Mini App відкриється!**

---

## 💅 Як працює система:

### Процес запису клієнта:

**Крок 1: Вибір майстра**
- Олена Коваленко (⭐ 4.9) - Класичний манікюр
- Марія Шевченко (⭐ 5.0) - Нарощування, дизайн
- Анна Бондаренко (⭐ 4.8) - Апаратний манікюр, SPA

**Крок 2: Вибір послуги**
- Класичний манікюр - 350 грн (60 хв)
- Апаратний манікюр - 450 грн (90 хв)
- Манікюр + дизайн - 500 грн (75 хв)
- Нарощування - 800 грн (120 хв)
- Педикюр - 450-500 грн
- SPA-манікюр - 600 грн (90 хв)
- Зняття покриття - 150 грн (30 хв)

**Крок 3: Вибір дати**
- Календар на 2 тижні вперед
- Сьогоднішня дата виділена

**Крок 4: Вибір часу**
- Слоти з 09:00 до 20:00
- Зайняті слоти неактивні (сірі)
- Минулі слоти неактивні

**Крок 5: Підтвердження**
- Введення телефону
- Коментар (необов'язково)
- Підтвердження запису

**Результат:**
```
✅ Запис успішно створено!

📅 Дата: 15 травня 2026
⏰ Час: 14:00
👩‍🎨 Майстер: Марія Шевченко
💅 Послуга: Апаратний манікюр
💰 Вартість: 450 грн
⏱ Тривалість: 90 хв

📍 вул. Хрещатик, 1, Київ

Ми надішлемо вам нагадування за день до візиту!
```

---

## 📊 Що зберігається в базі даних:

### Кожен запис містить:
- ID клієнта (Telegram user_id)
- Ім'я клієнта
- Телефон
- Майстер
- Послуга
- Дата та час
- Статус (pending/confirmed/cancelled)
- Коментар
- Час створення

### Переглянути записи:

```bash
cd C:\Users\kazmi\manicure-booking
sqlite3 data/bookings.db "SELECT * FROM bookings;"
```

---

## 🎨 Налаштування салону:

### Змінити дані салону:

Відредагуй `.env`:

```env
SALON_NAME=Твоя назва салону
SALON_PHONE=+380501234567
SALON_ADDRESS=Твоя адреса
WORK_START=09:00
WORK_END=20:00
```

### Додати нового майстра:

1. Відкрий `src/database.js`
2. Додай в масив `masters`:

```javascript
[4, 'Ім\'я Прізвище', '👩‍🎨', 'Спеціалізація', 5.0]
```

3. Видали файл `data/bookings.db`
4. Перезапусти бота (БД створиться заново)

### Додати нову послугу:

1. Відкрий `src/database.js`
2. Додай в масив `services`:

```javascript
[9, 'Назва послуги', 'Опис', 60, 400, 'Категорія']
```

3. Видали `data/bookings.db`
4. Перезапусти бота

---

## 📱 Команди бота:

### Для клієнтів:
- `/start` - Головне меню
- `/help` - Допомога
- `/mybookings` - Мої записи

### Кнопки:
- **📅 Записатися онлайн** - Відкриває Mini App
- **📋 Мої записи** - Показує всі записи клієнта
- **ℹ️ Про салон** - Інформація про салон

---

## 🔧 Керування ботом:

### Перезапустити бота:

```bash
# Зупинити
taskkill /F /IM node.exe

# Запустити
cd C:\Users\kazmi\manicure-booking
npm start
```

### Переглянути логи:

```bash
cat C:\Users\kazmi\manicure-booking\logs\bot-2026-05-09.log
```

### Перевірити процес:

```bash
tasklist | findstr node.exe
```

---

## 📈 Статистика:

### Переглянути всі записи:

```bash
sqlite3 data/bookings.db "SELECT * FROM bookings ORDER BY created_at DESC LIMIT 10;"
```

### Статистика по майстрам:

```bash
sqlite3 data/bookings.db "
SELECT m.name, COUNT(*) as total
FROM bookings b
JOIN masters m ON b.master_id = m.id
GROUP BY m.id;
"
```

### Найпопулярніші послуги:

```bash
sqlite3 data/bookings.db "
SELECT s.name, COUNT(*) as count
FROM bookings b
JOIN services s ON b.service_id = s.id
GROUP BY s.id
ORDER BY count DESC;
"
```

---

## 🎯 Наступні покращення:

### 1. Автоматичні нагадування
Додай в `src/bot.js`:

```javascript
import cron from 'node-cron';

// Щодня о 10:00 надсилати нагадування
cron.schedule('0 10 * * *', () => {
  const tomorrow = format(addDays(new Date(), 1), 'yyyy-MM-dd');
  const bookings = dbQueries.getBookingsByDate.all(tomorrow);

  bookings.forEach(booking => {
    bot.telegram.sendMessage(booking.user_id, `
🔔 Нагадування!

Завтра у вас запис:
⏰ ${booking.booking_time}
👩‍🎨 ${booking.master_name}
💅 ${booking.service_name}

Чекаємо на вас! 💖
    `);
  });
});
```

### 2. Google Calendar інтеграція
- Автоматично додавати записи в календар майстра
- Синхронізація зайнятих слотів

### 3. Адмін-панель
- Перегляд всіх записів
- Підтвердження/скасування
- Статистика

### 4. Онлайн-оплата
- Інтеграція з LiqPay/Fondy
- Передоплата при бронюванні

### 5. Система знижок
- Бонуси за кількість візитів
- Реферальна програма

---

## 📞 Підтримка:

### Якщо щось не працює:

1. **Бот не відповідає:**
   - Перевір чи запущений: `tasklist | findstr node`
   - Подивись логи: `logs/bot-2026-05-09.log`

2. **Mini App не відкривається:**
   - Перевір URL в @BotFather
   - Відкрий URL в браузері: https://webapp-xi-smoky.vercel.app

3. **Записи не зберігаються:**
   - Перевір чи існує `data/bookings.db`
   - Подивись логи бота

---

## ✨ Готово!

**Твоя система запису на манікюр повністю готова!**

### Що працює:
- ✅ Telegram бот
- ✅ Mini App з календарем
- ✅ База даних
- ✅ Логування
- ✅ 3 майстри, 8 послуг

### Залишилось:
- ⏳ Налаштувати Menu Button в @BotFather
- ⏳ Протестувати запис

**Іди в @BotFather і налаштуй Menu Button!** 🚀

---

**Проєкт:** `C:\Users\kazmi\manicure-booking\`  
**Mini App:** https://webapp-xi-smoky.vercel.app  
**Документація:** `README.md`
